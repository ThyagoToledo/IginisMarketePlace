'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function OrganizationCreateForm(){const router=useRouter();const[form,setForm]=useState({name:'',slug:'',description:''});const[result,setResult]=useState(null);const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);const r=await fetch('/api/organizations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await r.json();setBusy(false);if(r.ok)router.push(`/organizations/${data.organization.slug}/settings`);else setResult(data.error)}return <form className="panel form-section" onSubmit={submit}><h2>Dados públicos</h2><div className="form-grid"><label className="field">Nome<input required minLength={3} maxLength={80} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="field">Identificador<input minLength={3} maxLength={50} value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})} placeholder="gerado a partir do nome"/></label><label className="field field-full">Descrição<textarea maxLength={500} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label></div>{result&&<p className="result-err">{result}</p>}<button className="button button-primary" disabled={busy}>{busy?'Criando…':'Criar organização'}</button></form>}
